function login() {
    var pass = window.prompt('Введите пароль: ', '');
    if (pass==null || pass==''){
        console.log('Доступ запрещен')
}
    else{
        window.open(pass+'.html');
    }
}

function login() {
    var pass = window.prompt('Введите пароль: ', '');
    if (pass==null || pass==''){
        console.log('Доступ запрещен')
}
    else{
        window.open(pass+'.html');
    }
}